{
  "name": "rune-expedition",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "description": "An original mobile-first dark fantasy browser game demo",
  "scripts": {
    "test": "node tests/engine.test.js && node tests/balance.test.js",
    "build": "node build.mjs"
  },
  "devDependencies": {
    "esbuild": "0.28.2"
  }
}
